using System;

public class Flashcard
{
	public Flashcard()
	{
		//One flashcard will hold the term and definition, 
		string term;
		string definition;
	}
	private string ReturnAsString(string term, string definition);
	{
		//This will return the flashcard as a term and a definition
		return ($"{term}:{definition}")
	}
}
